package practice;

public class NC_Bracket_Matcher {
    /**
     * <ul>
     *     <li>
     *         <b>Bracket Matcher</b><br>
     *         Have the function BracketMatcher({@code str}) take the {@code str} parameter
     *         being passed and return 1 if the brackets are correctly matched and each one is
     *         accounted for. Otherwise, return 0.
     *     </li>
     *     <li>
     *         For example, if {@code str} is "(hello (world))", then the output should be
     *         1, but if {@code str} is "((hello (world))" the output should be 0 because
     *         the brackets do not correctly match up. Only "(" and ")" will be used as brackets.
     *         If {@code str} contains no brackets return 1.
     *     </li>
     *     <li>
     *         <b>Examples:</b><br>
     *         {@code
     *         Input: "(coder)(byte))"
     *         Output: 0
     *         } <br>
     *         {@code
     *         Input: "(c(oder)) b(yte)"
     *         Output: 1
     *         }
     *     </li>
     * </ul>
     */
    public static void main(String[] args) {
    }
}
